apiVersion: v1
kind: Service
metadata:
  name: carts
  namespace: carts
  labels:
    app: carts
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: carts